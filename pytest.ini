[pytest]
# `pythonpath = .` lets the tests import custom_components.ghost_mode without
# installing anything. `asyncio_mode = auto` is what
# pytest-homeassistant-custom-component expects.
pythonpath = .
asyncio_mode = auto
testpaths = tests
