{
  "config": {
    "step": {
      "user": {
        "title": "Set up Ghost Mode",
        "description": "Ghost Mode learns how your home looks while it is occupied and replays that pattern while you are away, so an empty house still looks lived-in. Submit to add it — it finds your lights, switches, covers and media players on its own."
      }
    },
    "abort": {
      "already_configured": "Ghost Mode is already set up."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Ghost Mode options",
        "description": "Ghost Mode finds its own entities. Use this only to drop ones it should ignore — typically device settings that are permanently on and that nobody outside can see.",
        "data": {
          "exclude": "Never learn or replay these"
        }
      }
    }
  },
  "entity": {
    "switch": {
      "ghost_mode": {
        "name": "Ghost Mode"
      }
    }
  },
  "services": {
    "learn_now": {
      "name": "Learn now",
      "description": "Read recorder history into the learned profile straight away, instead of waiting for the nightly run."
    }
  }
}
